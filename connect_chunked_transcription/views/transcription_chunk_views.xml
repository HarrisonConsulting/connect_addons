<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_transcription_chunk_list" model="ir.ui.view">
        <field name="name">connect.transcription.chunk.list</field>
        <field name="model">connect.transcription.chunk</field>
        <field name="arch" type="xml">
            <list string="Chunks" default_order="job_id, sequence">
                <field name="job_id"/>
                <field name="sequence"/>
                <field name="state"
                       decoration-success="state == 'done'"
                       decoration-danger="state == 'failed'"
                       decoration-warning="state == 'cancelled'"
                       decoration-info="state in ('pending','queued','in_progress')"
                       widget="badge"/>
                <field name="start_offset_seconds"/>
                <field name="duration_seconds"/>
                <field name="attempts"/>
                <field name="duration_ms"/>
                <field name="model"/>
            </list>
        </field>
    </record>

    <record id="view_transcription_chunk_form" model="ir.ui.view">
        <field name="name">connect.transcription.chunk.form</field>
        <field name="model">connect.transcription.chunk</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_retry" type="object"
                            string="Retry" class="btn-primary"
                            invisible="state not in ('failed','cancelled')"
                            groups="connect_chunked_transcription.group_connect_chunked_manager"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="pending,queued,in_progress,done"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="job_id"/>
                            <field name="sequence"/>
                            <field name="filename"/>
                            <field name="model"/>
                            <field name="response_format"/>
                        </group>
                        <group>
                            <field name="start_offset_seconds"/>
                            <field name="duration_seconds"/>
                            <field name="attempts"/>
                            <field name="duration_ms"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Transcript" name="transcript">
                            <field name="transcript_text" readonly="1"/>
                        </page>
                        <page string="Segments" name="segments">
                            <field name="transcript_segments" widget="ace"
                                   options="{'mode': 'json', 'readOnly': true}"/>
                        </page>
                        <page string="Error" name="error" invisible="not error_message">
                            <field name="error_message" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_transcription_chunk" model="ir.actions.act_window">
        <field name="name">Transcription Chunks</field>
        <field name="res_model">connect.transcription.chunk</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
